pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Optional / production swap-ins (not used in this offline sandbox build):
# duckdb            # local warehouse alternative to SQLite, closer to Snowflake semantics
# snowflake-connector-python  # real Snowflake destination (see src/load_to_snowflake.py)
# statsmodels       # SARIMA/ETS if you want library-based forecasting instead of src/'s
#                     hand-rolled Holt linear trend implementation
# boto3             # real S3 raw layer instead of data/raw/
# jupyter, nbformat # if you want to open/edit notebooks/*.ipynb interactively
